# AGENTS.md

- Keep the active-learning workflow process-agnostic: use `x`, `y`, and `id` in new code and docs.
- Use dependency-light Python unless a new dependency is clearly justified.
- Run `python3 -m unittest` after changing the proposal engine.
